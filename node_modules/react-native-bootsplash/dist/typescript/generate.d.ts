export declare const generate: ({ android, ios, workingPath, logoPath, backgroundColor, logoWidth, flavor, assetsPath, }: {
    android: {
        sourceDir: string;
        appName: string;
    } | null;
    ios: {
        projectPath: string;
    } | null;
    workingPath: string;
    logoPath: string;
    assetsPath?: string | undefined;
    backgroundColor: string;
    flavor: string;
    logoWidth: number;
}) => Promise<void>;
